{
  "name": "ghostline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-hostable agent workstation for controlled remote computer use.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/protocol && npm run build -w apps/api && npm run build -w apps/web",
    "dev": "npm run dev:api & npm run dev:web",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "e2e:api": "node scripts/e2e-api.mjs",
    "test": "vitest run packages/protocol/src/index.test.ts",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@vitejs/plugin-react": "latest",
    "typescript": "latest",
    "vite": "latest",
    "vitest": "latest"
  }
}
